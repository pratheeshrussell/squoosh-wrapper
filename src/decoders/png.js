/**
 * PNG decoder — re-exports the decode function from the PNG encoder/decoder module.
 * The squoosh_png codec handles both encoding and decoding.
 */
export { decode } from '../encoders/png.js';
export { default } from '../encoders/png.js';
